<Window x:Class="WpfApp.ClientInfoDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="ClientInfoDialog" Height="300" Width="400" WindowStartupLocation="CenterOwner">
    <Grid Margin="50,20" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Ім'я*" FontWeight="Bold"/>
        <TextBox x:Name="NameTextBox" Grid.Row="1" 
                 Text="{Binding AuthorName}"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 VerticalContentAlignment="Top"
                 Padding="10,8"/>
        
        <Label Content="Ел. пошта*" FontWeight="Bold"
               Grid.Row="2"/>
        <TextBox x:Name="EmailTextBox" Grid.Row="3" 
                 Text="{Binding AuthorEmail}"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 VerticalContentAlignment="Top"
                 Padding="10,8"/>

        <Button x:Name="SaveButton" Grid.Row="5"
                Content="Відправити"
                Command="{Binding SendFeedbackCommand}"
                Height="40"
                Margin="0,10,0,0"/>
    </Grid>
</Window>
