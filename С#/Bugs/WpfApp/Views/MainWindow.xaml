<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Bugs Manager" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="TextBoxSearch"
                     md:HintAssist.Hint="Пошук" 
                     md:HintAssist.IsFloating="False"
                     md:TextFieldAssist.UnderlineBrush="Black"
                     Padding="15,5"
                     FontSize="15"
                     Height="40"
                     BorderBrush="Black"
                     BorderThickness="0,0,0,1"/>

            <Button x:Name="ButtonSearch" 
                    Grid.Column="1" 
                    Background="Transparent" 
                    BorderBrush="Black"
                    BorderThickness="0,0,0,1"
                    md:ButtonAssist.CornerRadius="0"
                    md:ElevationAssist.Elevation="Dp0"
                    Padding="5,0"
                    Height="40">
                <md:PackIcon Kind="Search" 
                         Grid.Column="1"
                         Width="40" 
                         Height="40" 
                         HorizontalAlignment="Center" 
                         Foreground="Black"/>
            </Button>
            
            <Button x:Name="ButtonFilterActivate" 
                    Grid.Column="2" 
                    Background="Transparent" 
                    BorderBrush="Black"
                    BorderThickness="1"
                    md:ElevationAssist.Elevation="Dp0"
                    Padding="5,0"
                    Height="40">
                <md:PackIcon Kind="Filter" 
                             Width="40" 
                             Height="40" 
                             Foreground="DarkGoldenrod" />
            </Button>
            
        </Grid>

        <ItemsControl Grid.Row="1" ItemsSource="{Binding Tags}"
                      HorizontalAlignment="Stretch" Padding="10,5">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <CheckBox 
                        md:ColorZoneAssist.Mode="Custom"
                        Content="{Binding TagItem.Name}" 
                        IsChecked="{Binding IsSelected}"
                        Style="{StaticResource MyColorfulChip}"
                        Margin="4"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <ListBox Grid.Row="2" 
                 ItemsSource="{Binding Bugs}" 
                 HorizontalContentAlignment="Stretch"
                 BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="10,15">
                        <StackPanel>
                            <TextBlock Text="{Binding Error}" Foreground="#D32F2F" 
                               FontWeight="SemiBold" TextWrapping="Wrap" FontSize="14"/>

                            <TextBlock Margin="0,10,0,0" TextWrapping="Wrap" FontSize="13"
                               Visibility="{Binding ContextVisibility}">
                                <Run FontWeight="Bold" Text="Context: "/>
                                <Run Text="{Binding Context}"/>
                            </TextBlock>

                            <TextBlock Margin="0,10,0,0" TextWrapping="Wrap" FontSize="13">
                                <Run FontWeight="Bold" Text="Solution: "/>
                                <Run Text="{Binding Solution}"/>
                            </TextBlock>

                            <ItemsControl ItemsSource="{Binding Tags}" Margin="0,12,0,0">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{Binding AccentColor}" CornerRadius="12"
                                        Padding="10,2" Margin="0,0,6,4">
                                            <TextBlock Text="{Binding Name}" FontSize="12" Padding="5" Foreground="Black"/>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StatusBar Grid.Row="3">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>

            <StatusBarItem Grid.Column="0">
                <TextBlock VerticalAlignment="Center" Padding="10,0">
                    <Run Text="Total: "/>
                    <Run Text="{Binding BugsCount, Mode=OneWay}"/>
                </TextBlock>
            </StatusBarItem>

            <StatusBarItem Grid.Column="2">
                <Button Content="Add New" HorizontalAlignment="Right" Background="Red" Command="{Binding AddNewBugCommand}"/>
            </StatusBarItem>
        </StatusBar>

    </Grid>
</Window>
