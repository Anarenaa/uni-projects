<Window x:Class="Lab3.Forms.AddStudent"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab3.Forms"
        xmlns:userControls="clr-namespace:Lab3.UserControls"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="Add New Student" Height="380" Width="600">
    <Grid MaxWidth="500">
        <Grid.RowDefinitions>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="30*"/>
            <RowDefinition Height="30*"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <userControls:InputField x:Name="inputFieldStudentName" LabelText="Ім'я" Margin="0 0 5 0"/>
            <userControls:InputField x:Name="inputFieldStudentSurname" Grid.Column="1" LabelText="Прізвище" Margin="5 0 0 0"/>
            
        </Grid>
        <Grid Grid.Row="1" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="25"/>
                <RowDefinition Height="25"/>
            </Grid.RowDefinitions>

            <Label Content="Дата народження" VerticalAlignment="Bottom" />
            <DatePicker x:Name="datePickerStudentDateOfBirth" Grid.Row="1" VerticalContentAlignment="Center"/>
        </Grid>
        <Grid Grid.Row="2" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="25"/>
                <RowDefinition Height="70"/>
            </Grid.RowDefinitions>

            <Label Content="Інформація про учня" VerticalAlignment="Bottom" />
            <TextBox x:Name="txtStudentInfo" Grid.Row="1" FontSize="12" VerticalContentAlignment="Top" Padding="5" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" />
        </Grid>
        <Grid Grid.Row="3" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="15*"/>
                <ColumnDefinition Width="65*"/>
                <ColumnDefinition Width="20*"/>
            </Grid.ColumnDefinitions>

            <Label x:Name="labelForPhoto" Content="Фото: " VerticalAlignment="Bottom" />
            <Button x:Name="btnAddPhoto" Grid.Column="1" Content="+ Add Photo" Padding="10 0" HorizontalAlignment="Left" Click="btnAddPhoto_Click" />
            <Label x:Name="lblPhotoPath" Grid.Column="1" HorizontalAlignment="Left"/>
            <Button x:Name="btnSaveStudent" Grid.Column="2" Content="Save" Click="btnSaveStudent_Click"/>
        </Grid>
    </Grid>
</Window>
